class YOLOEngine:
    def __init__(self, model_path="yolov10n.pt"):
        self.model_path = model_path
        # self.model = YOLO(model_path) # Load model here

    def predict(self, frame):
        # results = self.model(frame)
        # return results
        return []
